name: bform
description: Package to allows you to create beautiful forms.
version: 0.2.3
homepage: https://github.com/gabrielgits/bform.git

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter